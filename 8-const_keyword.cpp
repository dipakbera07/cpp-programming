#include<iostream>
using namespace std;
int main(){
    const int a=7; //here we use this const key word to specify that this variable has a constant value and it can't be changed, if we try to change it it shows an error
   // a=6;  //here we make a try to change the value of the variable whis is constant so thats why it shows an error.
    cout<<"the value of a is: "<<a;
    return 0;
} 